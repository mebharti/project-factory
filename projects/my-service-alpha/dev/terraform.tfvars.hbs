# GCP Project Factory Configuration - DEV Environment
gcp_project_id     = "{{ values.projectName or projectName }}-dev"
environment        = "dev"
owner_team         = "{{ values.owner or owner }}"
gcp_region         = "{{ values.region or region }}"
billing_account_id = "012345-6789AB-CDEF01"
enable_base_vpc    = true
enable_iam_roles   = true
enabled_services   = [
  "{{ s }}",
  "{{ s }}",
  "{{ s }}",
  "{{ s }}"
]
status_flag        = "PROJECT_PENDING"
